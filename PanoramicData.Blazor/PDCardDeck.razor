@inherits PDComponentBase
@typeparam TCard

<div @attributes="GetDeckAttributes()" ondragover="event.preventDefault();">

	@foreach (var card in Cards)
	{
		<PDCard DraggingEnabled="IsEnabled"
				Template="CardTemplate"
				Css="CardCss"
				Card="card"
				ParentCardDeck="this"/>
	}
</div>

