@typeparam TItem

<div class="pd-column-selector">

    <PDDragContainer TItem="IDisplayItem"
        Items="_items"
        SelectionChanged="@((s) => OnSelectionChanged(s))">

        <PDDragPanel TItem="IDisplayItem"
            CanDrag="CanChangeOrder"
            ItemOrderChanged="@((a) => OnOrderChanged(a))" />

    </PDDragContainer>

</div>