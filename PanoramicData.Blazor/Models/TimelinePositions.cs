namespace PanoramicData.Blazor.Models;

/// <summary>
/// Specifies the horizontal alignment of content within a timeline bar segment.
/// </summary>
public enum TimelinePositions
{
	/// <summary>Aligned to the leading edge of the segment.</summary>
	Start,
	/// <summary>Centred within the segment.</summary>
	Center,
	/// <summary>Aligned to the trailing edge of the segment.</summary>
	End
}
