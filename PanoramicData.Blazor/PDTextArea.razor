<textarea id="@Id"
		  class="form-control @(IsVisible ? "" : "d-none ") @CssClass"
		  readonly="@(!IsEnabled)"
		  placeholder="@Placeholder"
		  rows="@Rows"
		  maxlength="@MaxLength"
		  @oninput="OnInput"
		  @onkeypress="OnKeypress">@Value</textarea>
